import { View } from "react-native";

interface Props {
  dice: number;
  sides: number;
  rollTrigger: boolean;
}

export default function DiceDisplay({ dice, sides, rollTrigger }: Props){
    return <View></View>
}