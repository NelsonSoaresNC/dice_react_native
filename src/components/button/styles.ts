import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        marginTop: 25,
        width: "100%",
        height: 52,
        backgroundColor: "#8d0101ff",
        justifyContent: "center",
        alignItems: "center",
        borderRadius: 10
    },
    tilte:{
        fontWeight: "bold",
        color: "#FFFFFF"
    }
});
